export enum LoggingSymbols {
  core = 'core',
  developers = 'developers',
  health = 'health',
  randomizer = 'randomizer',
  events = 'events',
  persistence = 'persistence',
  reports = 'reports',
}
